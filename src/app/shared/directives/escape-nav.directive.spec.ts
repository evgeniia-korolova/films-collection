import { EscapeNavDirective } from './escape-nav.directive';

describe('EscapeNavDirective', () => {
  it('should create an instance', () => {
    const directive = new EscapeNavDirective();
    expect(directive).toBeTruthy();
  });
});
